# Stowset

Minimal Next.js application prepared for Supabase integration.

## Development

Copy `.env.example` to `.env.local`, add Supabase credentials when available, then run:

```bash
pnpm install
pnpm dev
```
